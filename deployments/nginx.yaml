kind: Pod
metadata:
  name: busy-pod
spec:
  containers:
    - name: busybox
      image: busybox:latest

---

kind: Pod
metadata:
  name: caddy-pod
spec:
  containers:
    - name: caddy
      image: caddy:alpine
      ports:
      - containerPort: 80
